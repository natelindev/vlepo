## Shared function, constants between client and server

All code should be isomorphic and does not use client or server specific API directly.

No node_modules allowed, no yarn or npm install in this folder
